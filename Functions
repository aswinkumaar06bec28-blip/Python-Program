#Python programs using Functions
def maxi(a,b,c):
   if a>b and a>c:
      return a
   elif b>a and b>c:
      return b
   else:
      return c



a=int(input("Enter the number A : "))
b=int(input("Enter the number B : "))
c=int(input("Enter the number C : "))
print("The largest number is ",maxi(a,b,c))
